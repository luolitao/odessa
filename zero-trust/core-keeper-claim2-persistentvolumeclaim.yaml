apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: core-keeper-claim2
  name: core-keeper-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
