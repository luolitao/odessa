apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: edgex-init
  name: edgex-init
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
