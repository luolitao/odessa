apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: security-secretstore-setup-claim3
  name: security-secretstore-setup-claim3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
