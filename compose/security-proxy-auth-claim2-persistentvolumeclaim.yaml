apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: security-proxy-auth-claim2
  name: security-proxy-auth-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
