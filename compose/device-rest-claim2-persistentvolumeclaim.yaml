apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: device-rest-claim2
  name: device-rest-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
