apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: nginx-tls
  name: nginx-tls
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
