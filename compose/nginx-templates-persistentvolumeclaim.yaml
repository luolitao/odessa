apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: nginx-templates
  name: nginx-templates
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
