apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: support-notifications-claim2
  name: support-notifications-claim2
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
